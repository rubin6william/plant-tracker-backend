This is a laravel backend providing plant tracker api

## Getting Started

Start the docker container\
(This will start the development server on http://localhost:8080)

```bash
 ./vendor/bin/sail up -d
```

Run migrations

```bash
 ./vendor/bin/sail php artisan migrate
```

